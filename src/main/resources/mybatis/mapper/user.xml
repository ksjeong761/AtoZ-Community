<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atoz.user.UserMapper">
    <insert id="addUser" parameterType="com.atoz.user.UserEntity">
        INSERT INTO users (user_id, password, nickname, email, password_salt)
        VALUES (#{userId}, #{password}, #{nickname}, #{email}, #{passwordSalt});
    </insert>

    <select id="findById" resultType="com.atoz.user.UserEntity" parameterType="java.lang.String">
        SELECT user_id AS userId, password, nickname, email, password_salt AS passwordSalt
        FROM users
        WHERE user_id = #{userId};
    </select>
</mapper>